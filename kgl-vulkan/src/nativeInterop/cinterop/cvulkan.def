package = cvulkan
headers = vulkan/vulkan.h
headerFilter = vulkan/*.h

depends.osx = posix
depends.mingw = posix windows
depends.linux = posix

compilerOpts = -DVK_NO_PROTOTYPES

linkerOpts.mingw = -lvulkan-1
linkerOpts.linux = -L/usr/lib/x86_64-linux-gnu -lvulkan
linkerOpts.osx = -lvulkan
